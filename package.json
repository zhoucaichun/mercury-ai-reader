{
  "name": "mercury-ai-reader",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first AI reading assistant",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
