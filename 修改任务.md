# T7 Week 3 前端真实接入修改任务

T7 余婧：

你现在就在现有分支 `T7-week3-reader-ui-polish` 上继续修改，不用重新开新分支。

修改前请先把最新 `main` 合进你的分支，因为 T3 / T4 / T5 / T6 / T8 / T9 / T10 / T11 都已经合入 `main`。

建议先执行：

```bash
git switch T7-week3-reader-ui-polish
git pull origin T7-week3-reader-ui-polish
git fetch origin
git merge origin/main
```

如果有冲突，优先保留最新 `main` 的主链路和公共接口，不要覆盖已经合入的 T3 / T4 / T5 / T6 内容。

## 本次目标

把当前前端从“可展示的 UI”改成“真实可用的产品页面”。

完成后，产品页面上的 Summary / Translation / Usage / Export / Read / Save / Source 等功能都应基于当前真实文章运行，不再展示 `mock / demo / test` 数据或文案。

## 需要修改的内容

### 1. 删除所有 mock / demo / test 文案

当前页面里还有：

- `mock fallback`
- `Mock provider fallback`
- 英文版 `AI output currently uses a mock fallback...`

这些都不能出现在产品页面。

请改成产品化文案，例如：

- “请先配置模型服务”
- “当前使用本地备用 AI 通道”
- “模型服务未配置，无法生成真实摘要或翻译”

页面上不要出现 `mock / demo / test`。

### 2. 接入 T9 的真实 Provider 配置

当前 `week3AgentUiPort.ts` 里仍然写死了 `MockLLMProvider`。

你需要改成支持真实 Provider。

前端至少提供这些配置项：

- Base URL
- Model
- API Key
- Test connection
- Save

保存后用 T9 的：

```ts
createLLMProvider(config)
```

创建真实 Provider。

### 3. 支持学校 API

先只需要支持一个学校 API，不要求多模型管理。

配置项可以按：

```ts
{
  providerId: "school",
  providerName: "School Model",
  kind: "openai-compatible",
  baseUrl: 用户输入的学校 API 地址,
  model: 用户输入的模型名,
  apiKey: 用户输入的 key
}
```

注意：

- API key 只能存在本地；
- 不要写死在代码里；
- 不要提交到 GitHub。

### 4. Summary 使用真实 Provider

点击 Summary 时，必须使用当前选中文章的 `canonicalMarkdown`。

生成结果必须来自真实 Provider，不能显示固定样例。

要保证：

- 第二篇文章生成的是第二篇摘要；
- 第三篇文章生成的是第三篇摘要；
- 切换文章后不串文章。

### 5. Translation 使用真实 Provider

点击 Translation 时，也必须使用当前选中文章的 `canonicalMarkdown`。

翻译结果必须来自真实 Provider，不能显示固定样例。

要保证：

- 第二篇文章生成的是第二篇译文；
- 第三篇文章生成的是第三篇译文；
- 切换文章后不串文章。

### 6. Usage 展示真实调用记录

Summary / Translation / Test connection 调用后，Usage 区域要展示真实调用记录，包括：

- 调用类型；
- provider；
- model；
- status；
- token 数；
- latency / 时间。

如果还没有调用，显示真实空状态，不要显示假数据。

### 7. Export 导出当前文章

Export 使用 `main` 里已经统一好的接口：

```ts
exportCurrentArticle(input)
```

导出的 Markdown 必须对应当前文章，并包含：

- 标题；
- 原文链接；
- `canonicalMarkdown`；
- 已生成的摘要；
- 已生成的译文。

### 8. 状态不能串文章

点击下面这些操作后，不能跳回第一篇，也不能串到上一篇：

- Read / Unread；
- Save / Saved；
- Sync；
- Summary；
- Translation；
- Export；
- Source。

### 9. 保留 T6 的 canonicalMarkdown 链路

T6 的 Reader Pipeline 已经合入 `main`。

你只需要使用当前文章的 `canonicalMarkdown`，不要自己重新造正文字段。

## 提交前自测

PR 里请写清楚测试结果：

```bash
npm run build
npm test
npm run smoke:feed
npm run smoke:week2
```

页面也要手动测试：

- 输入真实 Feed URL；
- 默认订阅源同步；
- 选择第二篇或第三篇文章；
- 测试 Summary；
- 测试 Translation；
- 测试 Usage；
- 测试 Export；
- 测试 Read / Unread；
- 测试 Save / Saved；
- 确认页面没有 `mock / demo / test` 文案；
- 确认摘要、翻译、导出都不串文章。

## 最终要求

这次目标不是继续做原型，而是让前端页面成为真实可用的产品页面。

